# @package _global_
# Not actually building dataset. The num_train/num_test fields probably shouldn't be here,
# num_train/num_test fields here need to be in sync with the ones in the corresponding yaml file under compute_ntk folder
data:
    dataset: "cifar10"
    normalize: True
num_train: 5000
num_test: 1000

shape: [1, 32, 32, 3]
