# @package _global_
# Moved num_train and num_test inside data field in order for the dataset builder
# function to actually access the fields to build correct dataset size
data:
    dataset: "cifar10"
    normalize: True
    num_train: 500
    num_test: 500

shape: [1, 32, 32, 3]
